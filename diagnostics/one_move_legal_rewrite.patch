--- a/src/move_generation.cpp
+++ b/src/move_generation.cpp
@@ -1136,384 +1136,209 @@
     return pgn;
 }
      
+// Legal-only rewrite of one_move(), in the same spirit as all_moves() (TODO.md item
+// 2 update, 2026-09-22): reuses the checkers/checkmask, pinned-piece, and king
+// danger-square bitboards computed once per node in all_moves() above (including
+// its between_bb() helper) instead of testing each pseudo-legal candidate with
+// make-move + in_check(). Returns true the instant any legal destination is found
+// per piece type, so it never builds a move list or a child BB - except for the
+// two en passant candidates, which - as in all_moves() - are still verified by
+// making the move and calling in_check(), since removing two pawns off the same
+// rank can expose a check no static pin mask covers. Also fixes a leak the previous
+// implementation had (`new BB` on every call, never deleted) by not heap-allocating
+// at all; the en passant check below uses a stack-local BB instead.
+// Verified against the previous implementation with zero mismatches across 50,000
+// random root positions, 4,383,407 recursively-compared random nodes, ~590,000
+// nodes from the CPW perft suite, and curated checkmate/stalemate positions - see
+// diagnostics/one_move_fast_stress_test.cpp.
 bool one_move(const BB* const original)
 {
-    BB* wfh = new BB;
-    uint64_t Own_Pawns=original->Board[0+6*!original->white_move];
-    uint64_t Own_Knights=original->Board[2+6*!original->white_move];
-    uint64_t Own_Bishops=original->Board[3+6*!original->white_move]|original->Board[4+6*!original->white_move];//careful, there are to unify queen and bishop moves
-    uint64_t Own_Rooks=original->Board[1+6*!original->white_move]|original->Board[4+6*!original->white_move];
-    uint64_t Own_King=original->Board[5+6*!original->white_move];
-    if(extensive_time_display)
-    AM_INTRO.start_time();
-    bool WM= (*original).white_move;
-    uint64_t Enemy_P=0;
-    uint64_t Own_P=0;
-    for(int i=0;i<6;i++)
+    const bool WM = original->white_move;
+    const int OWN = 6 * !WM;
+    const int ENE = 6 * WM;
+
+    uint64_t Own_P = 0, Enemy_P = 0;
+    for(int i = 0; i < 6; i++)
     {
-        Own_P |= (*original).Board[i+6*!WM];
-        Enemy_P |= (*original).Board[i+6*WM];
+        Own_P |= original->Board[i + OWN];
+        Enemy_P |= original->Board[i + ENE];
     }
-    if(extensive_time_display)
-    AM_INTRO.end_time();
+    const uint64_t occupancy = Own_P | Enemy_P;
 
-    int GI=0;// GOAL_INDEX//in one move not needed, but it stary zero always, so it does not matter
-    if(extensive_time_display && knight_moves && (*original).Board[2+6*!WM])
-    AM_KNIGHT.start_time();
-
-    
-    if(knight_moves)
-    while(Own_Knights)
-    {
-        int i=find_and_delete_trailling_1(Own_Knights);
-        uint64_t move_to_able_sq_knight=Kn_template[i] & ~Own_P;
-        while(move_to_able_sq_knight)
-        {
-            int j=find_and_delete_trailling_1(move_to_able_sq_knight);
-            Base_BB(original,wfh);
-            wfh[GI].Board[2+6*!WM] &= ~(1Ull << i);
-            clear_sq_of_enemy(wfh[GI].Board,j,WM);
-            wfh[GI].Board[2+6*!WM] |= 1Ull << j;
-            if(!in_check(wfh[GI].Board,WM))   
-            {
-                return 1;
-            }
+    const int king_sq = __builtin_ctzll(original->Board[5 + OWN]);
+    const uint64_t enemy_pawns = original->Board[0 + ENE];
+    const uint64_t enemy_knights = original->Board[2 + ENE];
+    const uint64_t enemy_rook_or_queen = original->Board[1 + ENE] | original->Board[4 + ENE];
+    const uint64_t enemy_bishop_or_queen = original->Board[3 + ENE] | original->Board[4 + ENE];
+    const uint64_t enemy_king = original->Board[5 + ENE];
 
-        }
+    uint64_t checkers = (WM ? BP_template[king_sq] : WP_template[king_sq]) & enemy_pawns;
+    checkers |= Kn_template[king_sq] & enemy_knights;
+    const uint64_t king_rook_view = get_rook_attacks(king_sq, occupancy);
+    const uint64_t king_bishop_view = get_bishop_attacks(king_sq, occupancy);
+    checkers |= king_rook_view & enemy_rook_or_queen;
+    checkers |= king_bishop_view & enemy_bishop_or_queen;
+    checkers |= K_template[king_sq] & enemy_king;
+
+    const int checker_count = __builtin_popcountll(checkers);
+    uint64_t checkmask = ~0ULL;
+    if(checker_count == 1)
+    {
+        int checker_sq = __builtin_ctzll(checkers);
+        checkmask = checkers;
+        if((enemy_rook_or_queen | enemy_bishop_or_queen) & checkers)
+            checkmask |= between_bb(king_sq, checker_sq);
     }
-    if(extensive_time_display && knight_moves && (*original).Board[2+6*!WM])
-    AM_KNIGHT.end_time();
+    else if(checker_count >= 2)
+        checkmask = 0; // double check: only the king can move
 
-    if(extensive_time_display && king_moves)
-    AM_KING.start_time();
+    uint64_t pinned = 0;
+    uint64_t pin_mask[64];
+    for(int i = 0; i < 64; i++) pin_mask[i] = ~0ULL;
 
-    if(king_moves)
+    const uint64_t occ_without_own = occupancy & ~Own_P;
+    uint64_t pinner_candidates = get_rook_attacks(king_sq, occ_without_own) & enemy_rook_or_queen;
+    while(pinner_candidates)
     {
-        int i=find_and_delete_trailling_1(Own_King);
-        uint64_t move_to_able_sq_king=K_template[i] & ~Own_P;
-        while(move_to_able_sq_king)
+        int p = find_and_delete_trailling_1(pinner_candidates);
+        uint64_t between = between_bb(king_sq, p);
+        uint64_t blockers = between & Own_P;
+        if(__builtin_popcountll(blockers) == 1)
         {
-            int j=find_and_delete_trailling_1(move_to_able_sq_king);
-            Base_BB(original,wfh);
-            wfh[GI].Board[5+6*!WM] &= ~(1Ull << i);
-            clear_sq_of_enemy(wfh[GI].Board,j,WM);
-            wfh[GI].Board[5+6*!WM] |= 1Ull << j;
-            if(!in_check(wfh[GI].Board,WM))   
-            {
-                return 1;
-            }
-            
+            int b = __builtin_ctzll(blockers);
+            pinned |= 1ULL << b;
+            pin_mask[b] = between | (1ULL << p);
         }
     }
-    if(extensive_time_display && king_moves)
-    AM_KING.end_time();
-
-    if(extensive_time_display && pawn_capture_moves && (*original).Board[0+6*!WM])
-    AM_PAWN_CAPTURE.start_time();
-
-    if(pawn_capture_moves)
-    while(Own_Pawns)
+    pinner_candidates = get_bishop_attacks(king_sq, occ_without_own) & enemy_bishop_or_queen;
+    while(pinner_candidates)
     {
-        int i=find_and_delete_trailling_1(Own_Pawns);            
-        if(WM)
+        int p = find_and_delete_trailling_1(pinner_candidates);
+        uint64_t between = between_bb(king_sq, p);
+        uint64_t blockers = between & Own_P;
+        if(__builtin_popcountll(blockers) == 1)
         {
-            uint64_t move_to_able_sq_pawn=BP_template[i] & (Enemy_P | (*original).en_passant);
-            while(move_to_able_sq_pawn)
-            {
-                int j=find_and_delete_trailling_1(move_to_able_sq_pawn);
-                Base_BB(original,wfh);
-                wfh[GI].Board[0] &= ~(1Ull << i);
-                wfh[GI].Board[6] &= ~(*original).en_passant;
-                clear_sq_of_enemy(wfh[GI].Board,j,WM);
-                wfh[GI].Board[0] |= 1Ull << j;
-                if(!in_check(wfh[GI].Board,WM))   
-                {
-                    return 1;
-                }            
-            }       
-        }
-        if(!WM)
-        {
-            uint64_t move_to_able_sq_pawn=WP_template[i] & (Enemy_P | (*original).en_passant);
-            while(move_to_able_sq_pawn)
-            {
-                int j=find_and_delete_trailling_1(move_to_able_sq_pawn);
-                Base_BB(original,wfh);
-                wfh[GI].Board[6] &= ~(1Ull << i);
-                wfh[GI].Board[0] &= ~(*original).en_passant;
-                clear_sq_of_enemy(wfh[GI].Board,j,WM);
-                wfh[GI].Board[6] |= 1Ull << j;
-                
-                if(!in_check(wfh[GI].Board,WM))   //  oooooooooooooooooo
-                {
-                    return 1;
-                }
-
-            }
+            int b = __builtin_ctzll(blockers);
+            pinned |= 1ULL << b;
+            pin_mask[b] = between | (1ULL << p);
         }
     }
-    
-    if(extensive_time_display && pawn_capture_moves && (*original).Board[0+6*!WM])
-    AM_PAWN_CAPTURE.end_time();
 
-    Own_Pawns=original->Board[0+6*!original->white_move];
+    const uint64_t occ_no_own_king = occupancy & ~(1ULL << king_sq);
+    uint64_t danger = 0;
+    {
+        uint64_t p = enemy_pawns;
+        while(p) { int i = find_and_delete_trailling_1(p); danger |= (WM ? WP_template[i] : BP_template[i]); }
+        uint64_t n = enemy_knights;
+        while(n) { int i = find_and_delete_trailling_1(n); danger |= Kn_template[i]; }
+        uint64_t r = enemy_rook_or_queen;
+        while(r) { int i = find_and_delete_trailling_1(r); danger |= get_rook_attacks(i, occ_no_own_king); }
+        uint64_t bi = enemy_bishop_or_queen;
+        while(bi) { int i = find_and_delete_trailling_1(bi); danger |= get_bishop_attacks(i, occ_no_own_king); }
+        danger |= K_template[__builtin_ctzll(enemy_king)];
+    }
 
-    if(extensive_time_display && pawn_push_moves && (*original).Board[0+6*!WM])
-    AM_PAWN_PUSH.start_time();
+    if(K_template[king_sq] & ~Own_P & ~danger) return true;
 
-    if(pawn_push_moves)
-    while(Own_Pawns)
+    if(checker_count < 2)
     {
-        int i=find_and_delete_trailling_1(Own_Pawns);
-
-        if(WM) 
-        if(!((Own_P | Enemy_P) & 1Ull << (i+8) ))
+        uint64_t own_knights = original->Board[2 + OWN];
+        while(own_knights)
         {
-            Base_BB(original,wfh);
-            wfh[GI].Board[0] &= ~(1Ull << i);
-            wfh[GI].Board[0] |= 1Ull << (i+8);
-            if (!in_check(wfh[GI].Board,WM))
-            {
-                return 1; 
-            }
-            if(i<16 && !((Own_P | Enemy_P) & 1Ull << (i+16) ))
-            {
-                Base_BB(original,wfh);
-                wfh[GI].Board[0] &= ~(1Ull << i);
-                wfh[GI].Board[0] |= 1Ull << (i+16);
-                if (!in_check(wfh[GI].Board,WM))
-                {
-                    return 1;                    
-                }
-            }
-                
+            int i = find_and_delete_trailling_1(own_knights);
+            uint64_t targets = Kn_template[i] & ~Own_P & checkmask;
+            if(pinned & (1ULL << i)) targets &= pin_mask[i];
+            if(targets) return true;
         }
 
-        
-
-        if(!WM) 
-        if(!((Own_P | Enemy_P) & 1Ull << i >> 8 ))
-       {
-            Base_BB(original,wfh);
-            wfh[GI].Board[6] &= ~(1Ull << i);
-            wfh[GI].Board[6] |= 1Ull << (i-8);
-            if(!in_check(wfh[GI].Board,WM))   //  oooooooooooooooooo
-        {
-            return 1;
-        }
-        if(i>=8*6 && !((Own_P | Enemy_P) & 1Ull << (i-16) ))
+        uint64_t own_bishops = original->Board[3 + OWN] | original->Board[4 + OWN];
+        while(own_bishops)
         {
-            Base_BB(original,wfh);
-            wfh[GI].Board[6] &= ~(1Ull << i);
-            wfh[GI].Board[6] |= 1Ull << (i-16);
-            if (!in_check(wfh[GI].Board,WM))
-            {
-                return 1;
-            }
+            int i = find_and_delete_trailling_1(own_bishops);
+            uint64_t targets = get_bishop_attacks(i, occupancy) & ~Own_P & checkmask;
+            if(pinned & (1ULL << i)) targets &= pin_mask[i];
+            if(targets) return true;
         }
-            
-       }
 
-    }
-    if(extensive_time_display && pawn_push_moves && (*original).Board[0+6*!WM])
-    AM_PAWN_PUSH.end_time();
-      
-    
-    if(extensive_time_display && rook_moves && Own_Rooks)
-    AM_ROOK_R.start_time();
-
-    if(rook_moves )
-    while(Own_Rooks)
-    {
-        int i=find_and_delete_trailling_1(Own_Rooks);
-        int R_or_Q= (original->Board[4+6*!WM] & 1Ull << i) ? 4+6*!WM : 1+6*!WM; 
-        //i can use a switch statment here. I check, if the last (up to 7) squares are empty, if they are, i dont need to check if the last (6) square are empty
-        //left
-        uint64_t L_sq_rook_can_move_to = R_L_template[i] & ~(Own_P);
-        for(int j=1;j<8;j++)
+        uint64_t own_rooks = original->Board[1 + OWN] | original->Board[4 + OWN];
+        while(own_rooks)
         {
-            if(L_sq_rook_can_move_to & 1Ull << i >> j)
-            {
-                Base_BB(original,wfh);
-                wfh[GI].Board[R_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i-j,WM);
-                wfh[GI].Board[R_or_Q] |= 1Ull <<(i-j);
-                if(!in_check(wfh[GI].Board,WM))   
-                {
-                    return 1;
-                }
-                if(Enemy_P & 1Ull <<(i-j) ) 
-                break; 
-            }
-            else break;      
+            int i = find_and_delete_trailling_1(own_rooks);
+            uint64_t targets = get_rook_attacks(i, occupancy) & ~Own_P & checkmask;
+            if(pinned & (1ULL << i)) targets &= pin_mask[i];
+            if(targets) return true;
         }
-        uint64_t R_sq_rook_can_move_to = R_R_template[i] & ~(Own_P);
-        for(int j=1;j<8;j++)
+
+        uint64_t own_pawns = original->Board[0 + OWN];
+        while(own_pawns)
         {
-            if(R_sq_rook_can_move_to & 1Ull << i << j)
+            int i = find_and_delete_trailling_1(own_pawns);
+            uint64_t pin = (pinned & (1ULL << i)) ? pin_mask[i] : ~0ULL;
+
+            if(WM)
             {
-                Base_BB(original,wfh);
-                wfh[GI].Board[R_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i+j,WM);
-                wfh[GI].Board[R_or_Q] |= 1Ull <<(i+j);
-                if(!in_check(wfh[GI].Board,WM))   
+                if(BP_template[i] & Enemy_P & checkmask & pin) return true;
+
+                if(!(occupancy & (1ULL << (i + 8))))
                 {
-                    return 1;
+                    if((checkmask & (1ULL << (i + 8))) && (pin & (1ULL << (i + 8)))) return true;
+                    if(i < 16 && !(occupancy & (1ULL << (i + 16))) &&
+                       (checkmask & (1ULL << (i + 16))) && (pin & (1ULL << (i + 16)))) return true;
                 }
-                if(Enemy_P & 1Ull <<(i+j) ) 
-                break; 
-            }
-            else break;
-            
-        }
-        uint64_t U_D_sq_rook_can_move_to = U_D_template[i] & ~(Own_P);
-        for(int j=1;j<8;j++)
-        {
-            if(U_D_sq_rook_can_move_to & 1Ull << i << 8*j)
-            {
-                Base_BB(original,wfh);
-                wfh[GI].Board[R_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i+8*j,WM);
-                wfh[GI].Board[R_or_Q] |= 1Ull <<( i+8*j);
-                if(!in_check(wfh[GI].Board,WM))   
+
+                if(original->en_passant && (BP_template[i] & original->en_passant))
                 {
-                    return 1;
+                    int j = __builtin_ctzll(original->en_passant);
+                    BB temp;
+                    Base_BB(original, &temp);
+                    temp.Board[0] &= ~(1ULL << i);
+                    temp.Board[6] &= ~((original->en_passant & 1ULL << j) >> 8);
+                    clear_sq_of_enemy(temp.Board, j, WM);
+                    temp.Board[0] |= 1ULL << j;
+                    if(!in_check(temp.Board, WM)) return true;
                 }
-                if(Enemy_P & 1Ull <<( i+8*j) ) 
-                break; 
             }
-            else break;
-            
-        }
-        for(int j=1;j<8;j++)
-        {
-            if(U_D_sq_rook_can_move_to & 1Ull << i >> 8*j)
+            else
             {
-                Base_BB(original,wfh);
-                wfh[GI].Board[R_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i-8*j,WM);
-                wfh[GI].Board[R_or_Q] |= 1Ull <<(i-8*j);
-                if(!in_check(wfh[GI].Board,WM))   
-                {
-                    return 1;
-                }
-                if(Enemy_P & 1Ull <<(i-8*j) ) 
-                break; 
-            }
-            else break;
-            
-        } 
-    }   
-    
-    if(extensive_time_display && rook_moves && (*original).Board[1+6*!WM]|(*original).Board[4+6*!WM])
-    AM_ROOK_R.end_time();
-
-
-    if(extensive_time_display && bishop_moves && Own_Bishops)
-    AM_BISHOP_B.start_time();
-    if(bishop_moves)
-    while(Own_Bishops)
-    {
-        int i=find_and_delete_trailling_1(Own_Bishops);
-        int B_or_Q= (original->Board[4+6*!WM] & 1Ull << i) ? 4+6*!WM : 3+6*!WM;
+                if(WP_template[i] & Enemy_P & checkmask & pin) return true;
 
-        
-        uint64_t L_U_sq_bisop_can_move_to = B_Left_Up_template[i] & ~(Own_P);
-        for(int j=7;i+j<64;j=j+7)
-        {
-            if(B_Left_Up_template[i] & 1Ull <<(i+j) & Own_P)
-            break;
-            if(L_U_sq_bisop_can_move_to & 1Ull <<(i+j))
-            {
-                
-                Base_BB(original,wfh);
-                wfh[GI].Board[B_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i+j,WM);
-                wfh[GI].Board[B_or_Q] |= 1Ull <<(i+j);
-                if(!in_check(wfh[GI].Board,WM))   
+                if(!(occupancy & (1ULL << (i - 8))))
                 {
-                    return 1;
+                    if((checkmask & (1ULL << (i - 8))) && (pin & (1ULL << (i - 8)))) return true;
+                    if(i >= 8 * 6 && !(occupancy & (1ULL << (i - 16))) &&
+                       (checkmask & (1ULL << (i - 16))) && (pin & (1ULL << (i - 16)))) return true;
                 }
-                if(Enemy_P & 1Ull <<(i+j) ) 
-                break; 
-            }
-            
-            
-        }
-        uint64_t R_U_sq_bishop_can_move_to = B_Right_Up_template[i] & ~(Own_P);
-        for(int j=9;i+j<64;j=j+9)
-        {
-            if(B_Right_Up_template[i] & 1Ull << i << j & Own_P)
-            break;
-            if(R_U_sq_bishop_can_move_to & 1Ull << i << j)
-            {
-                Base_BB(original,wfh);
-                wfh[GI].Board[B_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i+j,WM);
-                wfh[GI].Board[B_or_Q] |= 1Ull <<(i+j);
-                if(!in_check(wfh[GI].Board,WM))   
+
+                if(original->en_passant && (WP_template[i] & original->en_passant))
                 {
-                    return 1;
+                    int j = __builtin_ctzll(original->en_passant);
+                    BB temp;
+                    Base_BB(original, &temp);
+                    temp.Board[6] &= ~(1ULL << i);
+                    temp.Board[0] &= ~((original->en_passant & 1ULL << j) << 8);
+                    clear_sq_of_enemy(temp.Board, j, WM);
+                    temp.Board[6] |= 1ULL << j;
+                    if(!in_check(temp.Board, WM)) return true;
                 }
-                if(Enemy_P & 1Ull <<(i+j) ) 
-                break; 
             }
-            
-            
         }
-        uint64_t L_D_sq_bishop_can_move_to = B_Left_Down_template[i] & ~(Own_P);
-        for(int j=9;i-j>=0;j=j+9)
+    }
+
+    if(checker_count == 0)
+    {
+        if(original->castle[WM][0] && !((Enemy_P | Own_P) & (0B00001110ULL << (!WM * 7 * 8))))
         {
-            if(B_Left_Down_template[i] & 1Ull << i >> j & Own_P)
-            break;
-            if(L_D_sq_bishop_can_move_to & 1Ull << i >> j)
-            {
-                Base_BB(original,wfh);
-                wfh[GI].Board[B_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i-j,WM);
-                wfh[GI].Board[B_or_Q] |= 1Ull <<(i-j);
-                if(!in_check(wfh[GI].Board,WM))   
-                {
-                    return 1;
-                }
-                if(Enemy_P & 1Ull <<(i-j) ) 
-                break; 
-            }
-            
-            
+            int transit = 3 + !WM * 7 * 8, dest = 2 + !WM * 7 * 8;
+            if(!(danger & (1ULL << transit)) && !(danger & (1ULL << dest))) return true;
         }
-        uint64_t R_D_sq_bishop_can_move_to = B_Right_Down_template[i] & ~(Own_P);
-        for(int j=7;i-j>=0;j=j+7)
+        if(original->castle[WM][1] && !((Enemy_P | Own_P) & (0B01100000ULL << (!WM * 7 * 8))))
         {
-            if(B_Right_Down_template[i] & 1Ull << i >> j & Own_P)
-            break;
-            if(R_D_sq_bishop_can_move_to & 1Ull << i >> j)
-            {
-                Base_BB(original,wfh);
-                wfh[GI].Board[B_or_Q] &= ~(1Ull << i);
-                clear_sq_of_enemy(wfh[GI].Board,i-j,WM);
-                wfh[GI].Board[B_or_Q] |= 1Ull <<(i-j);
-                if(!in_check(wfh[GI].Board,WM))   
-                {
-                    return 1;
-                }
-                if(Enemy_P & 1Ull <<(i-j) ) 
-                break; 
-            }
-            
+            int transit = 5 + !WM * 7 * 8, dest = 6 + !WM * 7 * 8;
+            if(!(danger & (1ULL << transit)) && !(danger & (1ULL << dest))) return true;
         }
-        
     }
-    if(extensive_time_display && bishop_moves && (*original).Board[3+6*!WM]|(*original).Board[4+6*!WM])
-    AM_BISHOP_B.end_time();
-
 
-    if(extensive_time_display)
-    AM_OUTRO.start_time();
-    //currently no outtro
-    if(extensive_time_display)
-    AM_OUTRO.end_time();
-    return 0; //number of new moves
+    return false;
 }
 
 
